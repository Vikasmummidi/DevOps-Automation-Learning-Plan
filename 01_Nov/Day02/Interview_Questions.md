# 🧠 Shell Tools — Interview Questions (4 Years Experience)

1. Difference between `grep`, `awk`, and `sed` in text processing?
2. How would you extract the 2nd column from a file using `cut`?
3. When would you prefer `awk` over `grep`?
4. How can you replace text recursively in files using `find` and `sed`?
5. What’s the use of `tr` and when is it better than `sed`?
6. Explain the difference between `find -name` and `find -type f`.
7. How do you find and delete all `.log` files older than 7 days?
8. Can you explain what `grep -v` does?
9. How do you combine multiple tools in a pipeline? Example: `grep | awk | sort`
10. How can you count unique occurrences of IPs in a log file using shell tools?
